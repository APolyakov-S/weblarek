https://github.com/APolyakov-S/weblarek.git


# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:

- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск

Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```

## Сборка

```
npm run build
```

или

```
yarn build
```

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

- Model - слой данных, отвечает за хранение и изменение данных.
- View - слой представления, отвечает за отображение данных на странице.
- Presenter - презентер содержит основную логику приложения и отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component

Является базовым классом для всех компонентов интерфейса. Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`

#### Класс Api

Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter

Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` - хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

## Данные

В приложении определены две основные сущности, описывающие бизнес-логику: **Товар** и **Покупатель**.

### Интерфейсы данных

**`IProduct`** — описывает структуру товара, полученного с сервера или используемого в приложении.

```typescript
interface IProduct {
  id: string;          // Уникальный идентификатор товара
  description: string; // Описание товара
  image: string;       // URL-адрес изображения товара
  title: string;       // Название товара
  category: string;    // Категория товара
  price: number | null;// Цена товара. null означает, что товар нельзя купить
}
```

**`IBuyer`** — описывает данные покупателя, необходимые для оформления заказа.

```typescript
type TPayment = 'card' | 'cash';

interface IBuyer {
  payment: TPayment | null;   // Способ оплаты: картой ('card') или наличными ('cash')
  email: string;              // Электронная почта
  phone: string;              // Номер телефона
  address: string;            // Адрес доставки
}
```

## Модели данных

Модели данных отвечают за хранение, изменение и предоставление данных приложения. Все модели реализуют событийный подход: при изменении данных они генерируют события, на которые подписываются другие части приложения.

### Класс `CatalogModel`

**Назначение:** управляет каталогом товаров. Хранит список всех доступных товаров и текущий выбранный товар для детального просмотра.

**Конструктор:**  
`constructor(events: EventEmitter)`

**Поля класса:**
- `private items: IProduct[]` — массив всех товаров в каталоге.
- `private preview: IProduct | null` — товар, выбранный для просмотра деталей.

**Методы класса:**
- `public setItems(items: IProduct[]): void` — сохраняет массив товаров. Генерирует событие `catalog:changed`.
- `public getItems(): IProduct[]` — возвращает массив всех товаров.
- `public getItem(id: string): IProduct | undefined` — возвращает товар по id.
- `public setPreview(item: IProduct): void` — сохраняет товар для просмотра. Генерирует событие `product:selected`.
- `public getPreview(): IProduct | null` — возвращает текущий выбранный товар.

### Класс `CartModel`

**Назначение:** управляет корзиной покупателя.

**Конструктор:**  
`constructor(events: EventEmitter)`

**Поля класса:**
- `private items: IProduct[]` — массив товаров в корзине.

**Методы класса:**
- `public getItems(): IProduct[]` — возвращает массив товаров в корзине.
- `public addItem(item: IProduct): void` — добавляет товар. Генерирует событие `basket:changed`.
- `public removeItem(itemId: string): void` — удаляет товар по id. Генерирует событие `basket:changed`.
- `public clear(): void` — очищает корзину. Генерирует событие `basket:changed`.
- `public getTotal(): number` — возвращает общую стоимость.
- `public getCount(): number` — возвращает количество товаров.
- `public hasItem(id: string): boolean` — проверяет наличие товара в корзине.

### Класс `BuyerModel`

**Назначение:** управляет данными покупателя при оформлении заказа.

**Конструктор:**  
`constructor(events: EventEmitter)`

**Поля класса:**
- `private payment: TPayment | null` — выбранный способ оплаты
- `private address: string` — адрес доставки
- `private email: string` — электронная почта
- `private phone: string` — номер телефона

**Методы класса:**
- `public update(data: Partial<IBuyer>): void` — обновляет данные покупателя. Генерирует событие `buyer:changed`.
- `public getData(): IBuyer` — возвращает все данные покупателя.
- `public clear(): void` — очищает данные. Генерирует событие `buyer:changed`.
- `public validate(): TBuyerErrors` — валидирует данные. Возвращает объект с ошибками.

### Слой коммуникации

Отвечает за взаимодействие приложения с сервером. Промежуточное звено между моделями данных и API-классом.

#### Интерфейс IProductResponse

Описывает ответ сервера при получении каталога товаров, содержит данные о списке товаров и их количество.

```TypeScript
interface IProductResponse {
  total: number; //общее количество товаров
  items: IProduct[]; //массив товаров, полученных с сервера
}
```

#### Интерфейс IOrderRequest

Интерфейс расширяет IBuyer и используется для формирования данных заказа, отправляемых на сервер.

```TypeScript
interface IOrderRequest extends IBuyer {
  total: number;
  items: string[];
}
```

#### Интерфейс IOrderResponse

Описывает ответ сервера после оформления заказа.

```TypeScript
interface IOrderResponse {
  id: string; //ID созданного заказа
  total: number; //итоговая сумма, подтвержденная сервером.
}
```

#### Класс CommunicationService

Реализует коммуникационный слой приложения и используют композицию с объектом, соответствующим интерфейсу IApi. Через этот объект класс выполняет HTTP-запросы к серверу.

Конструктор принимает объект, реализующий интерфейс IApi.

Методы класса:

`getProducts(): Promise<IProductResponse>` - Метод выполняет GET-запрос к эндпоинт /product/ . Получает с сервера объект, содержащий массив товаров.
`createOrder(data: IOrderRequest): Promise<IOrderResponse>` - Метод выполняет POST-запрос к эндпоинт /order/. Отправляет на сервер данные о заказе.

### Слой отображения

Отвечает за визуализацию данных в интерфейсе и обработку пользовательских событий.

#### Интерфейс IHeader

Описывает данные, необходимые для отображения состояния корзины в хедере.

```TypeScript
interface IHeader {
  counter: number; // количество товаров в корзине
}
```

#### Класс Header

Реализует слой отображения хедера. Отвечает за визуализацию счётчика товаров и уведомление приложения о действиях пользователя.

Конструктор принимает:
`container: HTMLElement` - корневой элемент хедера  
`events: IEvents` - объект брокера событий для генерации событий

Поля класса:

`protected counterElement: HTMLElement` - поле DOM-элемента для отображения количества товаров
`protected basketButton: HTMLButtonElement` - поле кнопки открытия корзины

Методы класса:

`set counter(value: number)` - обновляет отображение количества товаров в корзине

- обработчик события click на кнопке корзины - генерирует событие `basket:open`

#### Интерфейс IGallery

Описывает данные, необходимые для отображения списка карточек товаров.

```TypeScript
interface IGallery {
  items: HTMLElement[]; // массив DOM-элементов карточек товаров
}
```

#### Класс Gallery

Реализует слой отображения каталога товаров. Отвечает за отображение списка карточек в интерфейсе.

Конструктор принимает:
`container: HTMLElement` - корневой элемент каталога

Методы класса:

`set items(value: HTMLElement[])` - вставляет список карточек товаров в контейнер каталога

#### Интерфейс IModal

Описывает данные, необходимые для отображения модального окна.

```ts
interface IModal {
  content: HTMLElement; // DOM-элемент, который будет отображён внутри модального окна
}
```

#### Класс Modal

Реализует слой отображения модального окна. Отвечает за вставку контента, открытие и закрытие модального окна.

Поля:

`protected closeButton: HTMLButtonElement` - кнопка закрытия модального окна
`protected contentElement: HTMLElement` - контейнер для отображения переданного контента

Конструктор принимает:

`container: HTMLElement` - корневой элемент модального окна
`events: IEvents` - объект брокера событий для генерации событий

Методы класса:

`set content(value: HTMLElement)` - вставляет переданный контент в модальное окно  
`open()` - открывает модальное окно
`close()` - закрывает модальное окно и очищает контент
- обработчик события click на кнопке закрытия - вызывает метод `close()`
- обработчик события click по оверлею - вызывает метод `close()`

#### Интерфейс ISuccess

Описывает данные, необходимые для отображения успешного оформления заказа.

```ts
interface ISuccess {
  total: number; // итоговая сумма списанных синапсов
}
```

#### Класс Success

Реализует слой отображения экрана успешного оформления заказа. Отвечает за отображение итоговой суммы и обработку действия пользователя.

Поля:

`protected successDescription: HTMLElement` - элемент для отображения текста с итоговой суммой
`protected successButton: HTMLButtonElement` - кнопка возврата к покупкам

Конструктор принимает:

`container: HTMLElement` - корневой элемент блока успешного оформления  
`events: IEvents` - объект брокера событий для генерации событий

Методы класса:

`set total(value: number)` - обновляет текст с итоговой суммой списанных синапсов

- обработчик события click на кнопке - генерирует событие `success:close`

#### Интерфейс IBasket

Описывает данные, необходимые для отображения корзины.

```ts
interface IBasket {
  items: HTMLElement[]; // DOM-элементы товаров, добавленных в корзину
  total: number; // итоговая стоимость товаров в корзине
  buttonDisabled: boolean; // состояние доступности кнопки действия
}
```

#### Класс Basket

Реализует слой отображения корзины. Отвечает за отображение списка товаров, итоговой стоимости и уведомление о начале оформления заказа.

Поля:

`protected basketList: HTMLElement` - контейнер для списка товаров
`protected basketButton: HTMLButtonElement` - кнопка оформления заказа
`protected basketPrice: HTMLElement` - элемент для отображения итоговой стоимости

Конструктор принимает:
`container: HTMLElement` - корневой элемент корзины  
`events: IEvents` - объект брокера событий для генерации событий

Методы класса:

`set items(value: HTMLElement[])` - вставляет список товаров в контейнер корзины  
`set total(value: number)` - обновляет отображение итоговой стоимости товаров
`set buttonDisabled(value: boolean)` - включает или отключает кнопку действия

- обработчик события click на кнопке - генерирует событие `basket:submit`

---

#### Интерфейс `ICardBase`

Описывает общие данные карточки товара.

```ts
interface ICardBase {
  title: string; // название товара
  price: number | null; // цена товара
}
```

#### Абстрактный класс `CardBase`

Реализует общую часть отображения карточек товара. Используется как базовый класс для карточки каталога, карточки товара в модальном окне и карточки товара в корзине.

Поля:

`protected cardTitle: HTMLElement` - элемент для отображения названия товара
`protected cardPrice: HTMLElement` - элемент для отображения цены товара

Конструктор принимает:

`container: HTMLElement` - корневой элемент карточки

Методы класса:

`set title(value: string)` - обновляет название товара
`set price(value: number | null)` - обновляет цену товара

#### Интерфейс `ICardCatalog`

Описывает данные карточки товара в каталоге.

```ts
interface ICardCatalog extends ICardBase {
  category: string; // категория товара
  image: string; // ссылка на изображение товара
}
```

#### Тип `TCardCatalogActions`

Описывает обработчики действий пользователя в карточке каталога.

```ts
type TCardCatalogActions = {
  onSelect: () => void; // обработчик выбора товара
};
```

#### Класс `CardCatalog`

Реализует слой отображения карточки товара в каталоге. Отвечает за отображение категории, изображения, названия и цены товара, а также за обработку выбора карточки.

**Наследование:** `CardCatalog extends CardBase<ICardCatalog>`

Поля:

`protected cardCategory: HTMLElement` - элемент для отображения категории товара
`protected cardImage: HTMLImageElement` - элемент изображения товара

Конструктор принимает:

`container: HTMLElement` - корневой элемент карточки
`actions: TCardCatalogActions` - объект с обработчиком выбора товара

Методы класса:

`set category(value: string)` - обновляет текст категории и CSS-модификатор категории
`set image(value: string)` - обновляет изображение товара

- класс использует обработчик `onSelect`, передаваемый через объект действий `TCardCatalogActions`, для открытия полной карточки товара в модальном окне.

#### Интерфейс `ICardFull`

Описывает данные полной карточки товара для отображения в модальном окне.

```ts
interface ICardFull extends ICardBase {
  category: string; // категория товара
  description: string; // описание товара
  image: string; // ссылка на изображение товара
  buttonText: string; // текст кнопки действия
  buttonDisabled: boolean; // состояние доступности кнопки действия
}
```

#### Тип `TCardFullActions`

Описывает обработчики действий пользователя в полной карточке товара.

```ts
type TCardFullActions = {
  onButtonClick: () => void; // обработчик нажатия на кнопку
};
```

#### Класс `CardFull`

Реализует слой отображения полной карточки товара в модальном окне. Отвечает за отображение категории, изображения, описания, названия и цены товара, а также за обработку нажатия на кнопку.

**Наследование:** `CardFull extends CardBase<ICardFull>`

Поля:

`protected cardCategory: HTMLElement` - элемент для отображения категории товара
`protected cardImage: HTMLImageElement` - элемент изображения товара
`protected cardDescription: HTMLElement` - элемент для отображения описания товара
`protected cardButton: HTMLButtonElement` - кнопка действия

Конструктор принимает:

`container: HTMLElement` - корневой элемент полной карточки
`actions?: TCardFullActions` - опциональный объект с обработчиком нажатия на кнопку

Методы класса:

`set category(value: string)` - обновляет текст категории и CSS-модификатор категории
`set description(value: string)` - обновляет описание товара
`set image(value: string)` - обновляет изображение товара
`set buttonText(value: string)` - обновляет текст кнопки действия
`set buttonDisabled(value: boolean)` - включает или отключает кнопку действия

- класс использует обработчик `onButtonClick`, передаваемый через объект действий `TCardFullActions`

#### Интерфейс `ICardBasket`

Описывает данные карточки товара в корзине.

```ts
interface ICardBasket extends ICardBase {
  index: number; // порядковый номер товара в корзине
}
```

#### Тип `TCardBasketActions`

Описывает обработчики действий пользователя в карточке корзины.

```ts
type TCardBasketActions = {
  onDelete: () => void; // обработчик удаления товара из корзины
};
```

#### Класс `CardBasket`

Реализует слой отображения карточки товара в корзине. Отвечает за отображение порядкового номера, названия и цены товара, а также за обработку удаления товара из корзины.

**Наследование:** `CardBasket extends CardBase<ICardBasket>`

Поля:

`protected cardIndex: HTMLElement` - элемент для отображения порядкового номера товара
`protected deleteButton: HTMLButtonElement`- кнопка удаления товара из корзины

Конструктор принимает:

`container: HTMLElement` - корневой элемент карточки корзины
`actions: TCardBasketActions` - объект с обработчиком удаления товара

Методы класса:

`set index(value: number)` - обновляет порядковый номер товара в корзине

- класс использует обработчик `onDelete`, передаваемый через объект действий `TCardBasketActions`, для удаления товара из корзины.

##### Функция `setCategoryStyle` - располагается в src/utils/utils.ts

Обновляет текст категории товара и применяет CSS-модификатор для корректного отображения фона категории.

```ts
function setCategoryStyle(
  element: HTMLElement,
  value: string,
  categoryMap: Record<string, string>,
): void;
```

Параметры:

`element: HTMLElement` - DOM-элемент категории товара
`value: string` - название категории товара
`categoryMap: Record<string, string>` - объект соответствия названий категорий CSS

Описание:

- устанавливает текст категории
- сбрасывает предыдущие CSS-классы категории
- получает нужный CSS-модификатор из `categoryMap`
- добавляет найденный модификатор к элементу категории

#### Интерфейс `IFormState`

Описывает общее состояние формы.

```ts
interface IFormState {
  valid: boolean; // состояние валидности формы
  errors: string; // текст ошибки формы
}
```

#### Абстрактный класс `FormBase`

Реализует общую часть отображения форм. Используется как базовый класс для формы заказа и формы контактов.

Поля:

`protected form: HTMLFormElement` - корневой элемент формы
`protected submitButton: HTMLButtonElement` - кнопка отправки формы
`protected formErrors: HTMLElement` - элемент для отображения ошибок формы

Конструктор принимает:

`container: HTMLFormElement` - корневой элемент формы
`events: IEvents` - объект брокера событий для генерации событий

Методы класса:

`set valid(value: boolean)` - включает или отключает кнопку отправки формы
`set errors(value: string)` - обновляет текст ошибки формы

#### Интерфейс `IOrderForm`

Описывает данные формы выбора способа оплаты и адреса доставки.

```ts
interface IOrderForm {
  payment: TPayment | null; // выбранный способ оплаты
  address: string; // адрес доставки
}
```

#### Класс `OrderForm`

Реализует слой отображения формы заказа. Отвечает за выбор способа оплаты, ввод адреса доставки и генерацию событий при изменении данных формы.

**Наследование:** `OrderForm extends FormBase<IOrderForm>`

Поля:

`protected cardButton: HTMLButtonElement` - кнопка выбора оплаты онлайн
`protected cashButton: HTMLButtonElement` - кнопка выбора оплаты при получении
`protected addressInput: HTMLInputElement` - поле ввода адреса доставки

Конструктор принимает:

`container: HTMLFormElement` - корневой элемент формы заказа
`events: IEvents` - объект брокера событий для генерации событий

Методы класса:

`set payment(value: TPayment | null)` - обновляет визуальное выделение выбранного способа оплаты
`set address(value: string)` - обновляет значение поля адреса

- обработчик события `click` на кнопке оплаты онлайн - генерирует событие `order.payment:change` со значением `card`
- обработчик события `click` на кнопке оплаты при получении - генерирует событие `order.payment:change` со значением `cash`
- обработчик события `input` на поле адреса - генерирует событие `order.address:change`
- обработчик события `submit` формы - отменяет стандартную отправку формы и генерирует событие `order:submit`

#### Интерфейс `IContactsForm`

Описывает данные формы контактов покупателя.

```ts
interface IContactsForm {
  email: string; // email покупателя
  phone: string; // телефон покупателя
}
```

#### Класс `ContactsForm`

Реализует слой отображения формы контактов. Отвечает за ввод email и телефона, а также за генерацию событий при изменении данных формы.

**Наследование:** `ContactsForm extends FormBase<IContactsForm>`

Поля:

`protected emailInput: HTMLInputElement` - поле ввода email
`protected phoneInput: HTMLInputElement` - поле ввода телефона

Конструктор принимает:

`container: HTMLFormElement` - корневой элемент формы контактов
`events: IEvents` - объект брокера событий для генерации событий

Методы класса:

`set email(value: string)` - обновляет значение поля email
`set phone(value: string)` - обновляет значение поля телефона

- обработчик события `input` на поле email - генерирует событие `contacts.email:change`
- обработчик события `input` на поле телефона - генерирует событие `contacts.phone:change`
- обработчик события `submit` формы - отменяет стандартную отправку формы и генерирует событие `contacts:submit`

---

### Презентер

Презентер реализован в файле `src/main.ts` без выделения в отдельный класс. Логика презентера размещена в основном файле приложения и отвечает за связывание моделей данных и компонентов отображения по паттерну MVP.

Презентер:
- обрабатывает события пользовательского интерфейса;
- получает данные из моделей;
- передаёт данные в компоненты отображения;
- управляет открытием и закрытием модальных окон;
- инициирует запросы к серверу;
- обновляет отображение при изменении данных моделей.

Взаимодействие между слоями приложения построено через брокер событий `EventEmitter`, а также через передачу обработчиков действий (`actions`) в компоненты отображения карточек товаров.

Компоненты отображения генерируют события пользовательских действий, модели данных генерируют события изменения состояния данных, а презентер обрабатывает эти события и координирует работу приложения.

Основная логика приложения реализована через обработчики событий:

#### События моделей данных

- `catalog:changed` - изменение списка товаров каталога
- `product:selected` - изменение выбранного товара
- `basket:changed` - изменение содержимого корзины
- `buyer:changed` - изменение данных покупателя

#### События представлений

- `card:select` - выбор карточки товара для просмотра
- `card:action` - действие по кнопке в карточке товара (добавление/удаление)
- `basket:item-delete` - удаление товара из корзины
- `basket:open` - открытие корзины
- `basket:submit` - переход к оформлению заказа
- `order.payment:change` - изменение способа оплаты
- `order.address:change` - изменение адреса доставки
- `order:submit` - отправка первой формы заказа
- `contacts.email:change` - изменение email
- `contacts.phone:change` - изменение телефона
- `contacts:submit` - отправка заказа на сервер
- `success:close` - закрытие окна успешного оформления заказа
```